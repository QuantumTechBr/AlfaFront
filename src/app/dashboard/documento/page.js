// sections
import { FileManagerView } from 'src/sections/documento/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Documentos Administrativos',
};

export default function FileManagerPage() {
  return <FileManagerView />;
}
